{
  "name": "industrialdrone",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized Drone Fleet on Hardhat local node",
  "license": "MIT",
  "scripts": {
    "build": "echo 'Nothing to build'",
    "clean": "rimraf cache artifacts",
    "node": "hardhat node",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "demo:localhost": "hardhat run scripts/demo.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "rimraf": "^6.0.1"
  }
}
